language: go
go:
  - '1.x'

install:
  - make tools
  - pip install --user sphinx
  - pip install --user semantic-version

script:
  - make dist
  - ./build/coverage.sh

after_success:
  - bash <(curl -s https://codecov.io/bash)

